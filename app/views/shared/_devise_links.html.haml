.section.authenticate
  - if controller_name != 'sessions' 
    = link_to "Sign In", 
              new_user_session_path

  - if user_registerable? && controller_name != 'registrations'
    = link_to "Sign Up", 
              new_user_registration_path

  - if user_recoverable? && controller_name != 'passwords'
    = link_to "Forgot your password?", 
              new_password_path(User)

  - if user_confirmable? && controller_name != 'confirmations'
    = link_to "Didn't receive confirmation instructions?", 
              new_confirmation_path(resource_name)

  - if request.path != root_path
    = link_to "Back", root_path

